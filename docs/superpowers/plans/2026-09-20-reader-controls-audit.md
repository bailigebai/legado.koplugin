# v0.10.16 后续细节检查

目标：复现快速操作、异步目录、书签持久化及真实 KOReader 控件中的边界错误，最小修复后发布安装包。保留既有授权、用户数据和书源导入规则。

## 方案

1. 隐藏失效控件：省改动但损失可用操作。
2. 接通原有控件与侧栏模型，修复已复现状态错误（采用）：不引入新架构或依赖。
3. 重写侧栏：影响范围大，本次没有必要。

## 阶段

- 检查 side_toc.lua 的原生返回、首页/末页和页码跳转，使用真实 Menu 事件复现；修复只操作子控件导致侧栏未关闭的问题。
- 补目录乱序响应、失败与缓存重叠的测试，确保已加载数据和完成状态不会被旧响应遮挡。
- 独立只读检查 reader_session.lua 与 reader_sidebar.lua，按复现证据修复；不扩展功能。
- 运行相关回归、全量、原生兼容与跨章压力测试，审核后更新版本、打包并核对 GitHub 下载字节。

## 验收与边界

返回必须关闭整个侧栏；页码和箭头必须对应真实目录页；加载失败不隐藏已缓存章节；迟到请求不恢复已关闭界面或旧状态；书签失败保留数据与阅读位置。

主机测试验证逻辑、原生控件事件与资源回收。本轮若无 Kindle 连接与日志，不宣称实机零延迟。

## 本轮记录

- 已先失败再修复：真实 Close 事件只关闭嵌套 Menu；首页/末页及页码跳转使用单页总数；页脚全屏间距溢出；页码输入窗口在侧栏关闭后残留；旧部分响应覆盖完成标记；其他响应补齐后仍被 loading/error 遮挡。
- 运行代码改动集中于 side_toc.lua 和 reader_session.lua；新增 sidebar_controls_spec.lua（17 断言）、sidebar_request_order_spec.lua（7 断言）、prefetch_catalog_retry_spec.lua（6 断言）、native_close_progress_retry_spec.lua（10 断言），并补充快速连点书签的 3 个断言。
- 独立审查复现近末页目录预取失败永久抑制重试、原生关闭后待保存快照被忽略；均补先失败后通过的回归。另复现快速书签选择被先前目录回调抢先执行，复用 foreground_generation 拦截旧意图。
- 144 组 / 44,937 断言、原生 12 组 / 37,875 断言通过。日志：.tools/verification-01017-{full,native,soak}.log；跨章同窗复用 40/40，关闭后任务为 0。
- WPD 查询未识别到可用 Kindle，本轮不含实机触控及物理屏幕计时。
- 独立只读增量审查通过，定向 10 组 / 149 断言，无新的确认阻断。异常自定义 transport 的 cancel 抛错未在生产路径复现，未据此增加补丁。
- 最终压力测试 1,721 断言通过，500 次跨章与菜单/模式/休眠循环关闭后无遗留任务和目录引用。
- v0.10.17 已发布：提交 80ce28f，标签 v0.10.17；ZIP 124 项 / 420,503 字节。重复构建一致，SHA-256：620c648793a501a4988343153fcdc21f97440ee1e6a4fdeb96431b7da94b2a01。
