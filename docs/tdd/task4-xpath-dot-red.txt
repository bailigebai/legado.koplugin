Task 4 XPath current-node RED evidence
=====================================

Command:
  .tools\python\python.exe scripts\run_lua_specs.py --spec spec\rule_engine_spec.lua

Observed after adding the approved bare `.` current-node behavior:
  [FAIL] spec\rule_engine_spec.lua
  [RED] xpath axes, values, and approved predicates
  expected AlphaNewBeta, got table: <node>
  Specs: 0 passed, 1 failed; Assertions: 0

Root cause: the XPath `.` fast path returned the internal parser node rather
than applying the rule engine's scalar text contract. The minimal fix routes
the current node through the same text extraction used by other XPath node
steps.
